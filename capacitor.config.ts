import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fifthdimension.landing',
  appName: '5D Landing Page',
  webDir: 'dist'
};

export default config;
